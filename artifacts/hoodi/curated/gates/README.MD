Underling folders contain up-to-date information about participant trees used in the Curated Gates.

The participant trees are generated using `compose.js` script form the `.csv` files in the corresponding folders.
