The fixtures for the tests of Verifier are provided dynamically by invoking the corresponding \*.mjs files. See the
`_loadFixture` function in the corresponding tests. The scripts provide an ABI-encoded fixture structure expected by the
tests. The ABIs are extracted directly from the artifacts generated by the compiler via `forge build`. The corresponding
test contracts should expose an external function `ffi_interface` accepting a fixture as input.
